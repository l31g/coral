#cordbconn
server="server";
user="server";
password="server";
port="server";
DBName="server";
type="server";
#enddbconn

#cordb
#enddb

void main() {

	a = 1;

	if (a > 5) {
		a = 4;
		return a;
	} else {
		return a;
	}
}

#cordbconn
server="server";
user="server";
password="server";
port="server";
DBName="server";
type="sqlite";
#enddbconn

#cordb
#enddb

void main() {

	int a = 1;

	if (1 && 1) {
		a = 2;
		printf(a);
		printf("\n");
	}
	if (1 || 0) {
		a =1; 
		printf(a);
		printf("\n");
	}
	if (a > 5 ) {
		a = 4;
		printf(a);
		printf("\n");
	}
	printf("derpin");
}
